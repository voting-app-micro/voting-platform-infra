# Local Terraform working state
**/.terraform/*
*.tfstate
*.tfstate.*
crash.log
crash.*.log

# Override files (local-only overrides, never committed)
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# CLI config
.terraformrc
terraform.rc
